# Template for MSR with Central Users, CDS, JDBC and MQ Adapters

## Constants

This template uses Oracle database for the CDS, CU and Internal jdbc pools

Due to an issue with the CDS setup, we have manually added `e2ei/11/WIS_10.15.0.0.LATEST/Infrastructure/WISSharedBundles` in the `InstallProducts` product list.



